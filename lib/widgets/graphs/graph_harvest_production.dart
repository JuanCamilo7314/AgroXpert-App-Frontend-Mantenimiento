import 'package:flutter/material.dart';

class GraphHarvestProduction extends StatelessWidget {
  const GraphHarvestProduction({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
